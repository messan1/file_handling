/// <reference types="node" />
import { Transform, TransformCallback } from "stream";
import { Hash } from "crypto";
/**
 * Passes through its contents and ammends a hex-encoded hash of the data upon "end".
 *
 * @internal
 */
export default class AppendHashStream extends Transform {
    hash: Hash;
    constructor(algorithm?: string);
    _transform(chunk: Buffer, _: BufferEncoding, callback: TransformCallback): void;
    _flush(): void;
    private _digest?;
    digest(): string;
}
//# sourceMappingURL=append-hash-stream.d.ts.map